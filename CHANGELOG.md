# Changelog

本项目最初没有启用 Git，因此早期迭代没有真实提交快照。下面的版本记录根据对话历史和当前代码状态重建，用于说明功能演进脉络；其中当前代码对应最新版本。

## v1.0.20 - 回顾结束与掌握状态拆分

更新日期：2026-06-06

- 前端状态拆分为 `completed` 和 `mastered`：`completed` 表示本轮是否结束，`mastered` 表示是否达到自动掌握标准。
- 达到掌握标准后不再强制终止回顾；用户仍可继续下一题、换题、追问或切换主观题。
- 手动点击“结束回顾”只结束本轮，不再把未达标记录标记为已掌握。
- 最近回顾标签区分“已掌握”“已结束”和“复习中”。
- 从左侧最近回顾点击“重点回顾”时，会立即切换到目标知识点并清空旧题目、解析、追问和选项状态。
- 重点回顾的薄弱点归纳、题库生成、补题、追问和客观题错因分析加入会话版本保护，避免旧异步请求覆盖新会话界面。

## v1.0.19 - 客观题交互状态一致性修复

更新日期：2026-06-06

- 将页面初始客观题进度从旧的 `0/12` 修正为 `0/100`，与题库优先复盘规则一致。
- 恢复已答题解析页时，优先从本地题库快照恢复题干、选项和标准答案，避免客观题恢复后退化为简答题视图。
- 客观题提交和“我不会”流程统一使用本地掌握标准，达到题量、覆盖、正确率和最近表现条件后可直接进入完成状态。
- “换一题”在题库末尾时会先尝试补题，再推进到补出的下一题，避免回退到旧的模型临时出题路径。

## v1.0.18 - 重点回顾子点覆盖策略优化

更新日期：2026-06-06

- 重点回顾会按 `knowledgeAspect + focusSubpoint` 分组统计薄弱题，而不是只按内容方面汇总。
- 前端结构化信号新增 `subpointStats`，包含每个薄弱子点的题量、弱项数量、平均分、混淆点和样例题。
- `weakFocus` 模型归纳新增 `targetSubpoints`、`misconceptions` 和 `questionStrategies`。
- 生成重点题库时，`bankQuestions` 会直接接收 targetSubpoints、misconceptions 和出题策略，优先围绕薄弱子点和混淆点出题。
- 后端出题提示词要求重点回顾优先覆盖 targetSubpoints，并用 misconceptions 设计辨析和干扰项。

## v1.0.17 - 重点回顾薄弱点模型归纳

更新日期：2026-06-06

- 重点回顾不再只由前端从单题反馈中拼接薄弱点 notes。
- 前端会先从历史中筛选并结构化错题、低分内容方面、错误点、遗漏点、判断策略和内容方面统计。
- 后端新增 `weakFocus` 模式，让模型综合归纳真正的薄弱概念、优先级、重点内容方面和复习摘要。
- 重点题库规划会优先使用模型归纳出的薄弱方面；模型归纳失败时回退到本地拼接策略，保证功能不中断。

## v1.0.16 - 公网部署访问控制

更新日期：2026-06-05

- 后端新增 `REVIEW_ACCESS_PASSWORD`，配置后所有 `/api/review` 请求必须提供正确访问密码。
- 后端新增 `DAILY_REQUEST_LIMIT`，按 IP 做每日请求次数限制，降低 DeepSeek 额度被滥用的风险。
- 前端新增访问密码输入框，并在浏览器本地保存，后续请求自动携带。
- 新增 `render.yaml`，支持在 Render 上直接创建 Node Web Service。
- README 增加公开部署步骤、环境变量配置和访问控制说明。

## v1.0.15 - 单选题正确答案表述修正

更新日期：2026-06-05

- 单选题和判断题答错时，不再把未选中的正确答案表述为“漏选”。
- 单选题和判断题改为展示“正确答案是 B。B 正确：...”。
- 多选题仍保留“你漏选了 B”这类表述。
- 同步更新模型错因分析提示词和本地兜底文案，保证两条路径表述一致。

## v1.0.14 - 关键依据与下次判断策略展示优化

更新日期：2026-06-05

- 客观题答错后，关键依据区域也先显示“正在分析关键依据...”，等待模型返回后再展示最终依据。
- 错误点 / 遗漏点卡片会追加展示模型返回的 `nextTimeStrategy`，作为“下次判断”提示。
- 模型分析失败时恢复本地基础依据和基础错因，避免卡片停留在加载态。

## v1.0.13 - 回顾模式流转回归修复

更新日期：2026-06-05

- 对客观题追问返回、客观题切主观题再切回客观题、主观题提交后切回客观题、换一题、我不会、下一题等流程做回归测试。
- 修复客观题点击“我不会”后错因区域进入加载态但没有触发模型错因分析的问题。
- 重置会话时同步清理客观题错因分析去重 key，避免旧状态影响新会话。

## v1.0.12 - 错因分析加载态优化

更新日期：2026-06-05

- 客观题答错后不再先展示本地错误点 / 遗漏点再被模型结果覆盖。
- 提交后立即展示对错、得分、标准答案、关键依据和选项解析。
- 错误点 / 遗漏点区域先显示“正在分析错因...”，等待 `objectiveFeedback` 返回后再展示最终错因。
- 模型分析失败时才使用本地基础错因兜底，避免用户看到两版错因。

## v1.0.11 - 客观题错因分析去重

更新日期：2026-06-05

- 修复客观题提交后仍可再次提交同一道题的问题，避免重复触发模型错因分析。
- “提交回答”按钮在解析页会被禁用，只有进入下一题后才重新可用。
- 客观题错因分析增加同题同答案去重 key，防止重复请求返回两份分析。

## v1.0.10 - 本题解析卡片布局优化

更新日期：2026-06-05

- 本题解析中的“关键依据”和“错误点 / 遗漏点”改为与“结论”一致的横向卡片。
- 解析区从两列网格改为单列堆叠，避免内容长短不一导致左右不对称。
- 保持移动端和桌面端使用同一套自适应卡片布局。

## v1.0.9 - 客观题错误点与遗漏点解释优化

更新日期：2026-06-05

- 客观题答错后，“错误点 / 遗漏点”不再只显示答案对照和漏选数量。
- 客观题先用本地标准答案即时判分，再异步调用模型补充更精准的错因分析。
- 模型会结合题干、选项、标准答案、学习者选择和已有选项解释，说明错选项为什么不应选、漏选项为什么应该选。
- 模型返回后自动更新当前题的错误点、遗漏点、关键依据和下次判断条件；如果用户已进入下一题则丢弃返回结果。
- 模型分析失败时保留本地基础解析，不影响继续做题。
- 题库生成提示词强化选项解释要求：正确选项说明判断条件，错误选项说明混淆点。

## v1.0.8 - 主观题解析与题型切换修复

更新日期：2026-06-05

- 修复从客观题切换到主观题后，主观题提交解析仍显示选择题选项解析的问题。
- 主观题评估结果会强制清理客观题字段，包括选项、标准答案、选项解析和客观题正确性标记。
- 刷新恢复旧会话时，如果上一题是主观题，也会清理已缓存的客观题残留解析。
- 主观题作答后，如果题库仍存在，题型切换按钮允许用户切回客观题继续作答。
- 切回客观题时优先恢复题库当前位置，避免落入旧的动态出题路径。

## v1.0.7 - 下一题允许反复补题

更新日期：2026-06-03

- 撤销补题失败后禁用“下一题”的锁定策略。
- 当题库未满目标且没有现成下一题时，“下一题”会始终允许再次尝试补题。
- 补不到题时仍会提示原因，但不会阻止用户继续点击重试。

## v1.0.6 - 补题时扩展内容方面

更新日期：2026-06-03

- 当已有内容方面补不到不相似新题时，系统会先扩展新的知识点内容方面，而不是继续硬补旧方面。
- 新增 `bankExpandPlan` 模式，用于基于已有内容方面和已有题干生成 3 到 5 个新增内容方面。
- 扩展出的新内容方面会合并进 `coveragePlan`，并带上各自子点清单。
- 补题流程会在扩展成功后从新内容方面继续生成题目。
- 只有扩展内容方面也失败，或扩展后仍补不到有效新题，才显示“暂时没有补到新题”。

## v1.0.5 - 补题失败后禁用下一题

更新日期：2026-06-03

- 增加补题失败锁定状态，避免面板显示“暂时没有补到新题”时“下一题”按钮仍可点击。
- 只有补进新题、进入新题、重新生成题库或重置会话时，才会清除补题失败锁定。
- 补题失败状态会保存到本地会话，刷新后按钮状态仍保持一致。

## v1.0.4 - 补题状态收口

更新日期：2026-06-03

- 修复补题失败后进度面板仍显示“正在补题”的状态不一致问题。
- 补题失败或补不到新题时，进度面板会同步显示明确失败状态和原因。
- 修正补题候选方面选择：优先补未满 10 题的内容方面，不再优先补已经完成 10 题的方面。
- 进度面板新增“已有 N 题”状态，避免把过滤后不足 10 题的方面误显示为“完成”。

## v1.0.3 - 批量补题缓冲

更新日期：2026-06-03

- “下一题”触发补题时，不再只补最多 5 道题。
- 补题改为批量缓冲策略：一次尽量补出最多 20 道有效题，减少反复补题。
- 单个内容方面最多补 10 道，不足时继续尝试其他未覆盖方面。
- 补题仍会经过相似题过滤和子点去重，实际入库题数以过滤后结果为准。

## v1.0.2 - 子点覆盖与相似题过滤

更新日期：2026-06-03

- 题库规划从“内容方面”扩展为“内容方面 + 子点清单”，每个内容方面要求拆出 10 个可出题子点。
- 生成题目时要求每道题绑定 `focusSubpoint`，避免同一内容方面内反复考同一个子点。
- 后端增加题干相似度检测，过滤完全重复、包含式重复和高相似度题目。
- 前端在接收题目后再次执行相似题过滤和子点去重，防止相似题进入本地题库。
- 补题请求会携带已覆盖子点和已有题干，要求模型优先补尚未覆盖的子点。

## v1.0.1 - 题库生成进度与 JSON 修复

更新日期：2026-06-03

- 增加醒目的题库生成进度面板，展示当前内容方面、已完成题数、总进度条和每个方面的生成状态。
- 题库生成从每方面一次 10 题改为每方面两批、每批 5 题，降低单次模型 JSON 过长导致格式错误的概率。
- 后端对题库生成结果增加 JSON 修复流程：解析失败时自动请求模型只修复 JSON 语法。
- 如果某一批题目修复失败，前端会跳过该批并继续后续方面，不再直接中断整轮题库生成。
- 某个内容方面不足 10 题时，前端会自动补题，最多补两次。

## v1.0.0 - 题库优先复盘主线

更新日期：2026-06-03

- 开始回顾时不再逐题临时生成问题，改为先生成该知识点的完整题库。
- 题库生成拆成两步：先生成该知识点的具体内容方面清单，再按每个内容方面分批生成客观题。
- 默认按 10 个内容方面、每方面 10 道客观题规划，总目标提升到 100 道以上。
- 题库内容方面要求是关键词的具体知识构成，禁止“定义/机制/应用”等泛标签和空泛阶段模板。
- 后端新增题库规划和分方面出题模式，并过滤泛化题干。
- 服务端继续校正“多选题只有一个正确答案”的情况，避免错误题型进入题库。
- 客观题提交后改为前端本地判分，立即展示结论、关键依据、错误点和选项解析。
- “下一题”从本地题库按顺序推进，不再每题等待模型请求。
- 追问答疑不再阻断主线回顾；追问界面可以继续提问，也可以直接点击“下一题”。
- “换一题”优先从当前题库切到下一题，避免重新触发旧的动态出题路径。
- 刷新恢复时以题库当前位置为准，旧缓存停在简答题时会回到题库客观题。
- 掌握情况展示改为基于题库总量、整体正确率、各内容方面题量和内容方面正确率。
- “最近回顾”的重点回顾会提取历史薄弱内容方面、错误点和遗漏点，用于重新生成重点题库。
- README 同步更新为题库优先、100 题目标和按内容方面统计掌握情况的规则。

## v0.9.0 - 内容方面覆盖式出题

更新日期：2026-06-01

- 追问模式点击“继续回顾”返回问答模式时，状态提示同步刷新为内容方面掌握情况，避免沿用旧提示。
- 掌握情况提示改为按内容方面题量展示，优先说明哪些内容方面未达到每方面 10 题，而不是只显示总客观题数。
- 修复追问返回后继续使用旧 `pendingReview` 下一题的问题；点击“下一题”会重新按当前内容覆盖计划向后端生成题目。
- 切回客观题的兜底题改为围绕当前内容方面生成单选题，避免回到旧的通用阶段模板。
- 移除会暴露给用户的通用兜底题模板，避免出现“关于某阶段，需要区分核心原理、适用边界和常见误区”这类空泛题干。
- 提示词明确禁止空泛模板题，要求题干必须包含该知识点的具体概念、结构、过程、对比对象或应用场景。
- 出题策略从通用阶段覆盖改为知识点内容方面覆盖，要求模型为当前关键词建立 `coveragePlan`。
- 每个内容方面至少完成 10 道客观题；全部内容方面覆盖前，优先补齐未覆盖或题量不足的方面。
- 所有内容方面覆盖后，才回到薄弱方面继续出题，并要求换子点、边界、场景或混淆项，避免相似重复题。
- 掌握判定更新为至少 6 个核心内容方面、每方面 10 道客观题、总计至少 60 道客观题。
- 用户提交回答后的掌握情况提示改为展示内容方面覆盖，而不是通用阶段覆盖。

## v0.8.2 - 追问返回状态修复

更新日期：2026-06-01

- 用户提交回答后，状态提示会追加一句当前掌握情况，概括客观题进度、正确率、内容覆盖、最近表现和风险点。
- 简答题状态下，“主观题”按钮会变为“客观题”，用户可主动切回客观题继续复盘。
- 服务端新增切回客观题模式，强制生成判断题、单选题或多选题，避免继续沿用简答题路径。
- 题型切换按钮使用独立启用规则，不再被主观题回答框状态误锁住。
- 修复多选题只有一个标准答案的问题：服务端要求多选题至少两个正确答案，否则自动按单选题展示。
- 前端增加兜底校正：旧缓存或已生成题中如果“多选题”只有一个标准答案，会在显示前改为“单选题”。
- 修复追问模式点击“继续回顾”后，题目题型可能变成简答题但解析仍是客观题的状态错位问题。
- 提交回答时单独保存刚作答题的题型、选项、标准答案和已选答案。
- 返回解析页或恢复会话时，优先使用刚作答题快照恢复题目界面，确保题目和解析一致。
- 兼容旧缓存：如果刷新时缺少刚作答题快照，会从本题选项解析中反推客观题选项、正确答案和已选答案，并写回本地会话。
- 进一步兼容更旧的会话缓存：如果缺少 `lastAnsweredQuestion`，会从最后一条正式回顾记录中恢复刚作答题。

## v0.8.1 - 追问显示精简

更新日期：2026-06-01

- 追问模式下只显示当前最新追问和回答。
- 用户发起新的追问时，会覆盖上一轮追问展示，不再保留历史追问记录。
- 追问内容仍不计入正式回顾记录，也不会推进到下一题。
- README 中的掌握判定说明同步为后端当前定量规则：30 道客观题、85% 正确率、至少 5 个阶段、最近 3 轮稳定表现和无明显错误风险。

## v0.8.0 - 追问答疑与本题视角修正

- 修正解析文案中“上一题”残留，统一为“本题”视角。
- 提交回答后支持进入“追问答疑”模式。
- 追问答疑不推进题目进度，用户可通过“继续回顾”返回本题解析。
- 服务端增加 `followupAnswer` 返回字段，用于围绕本题解析回答用户追问。

## v0.7.0 - 本题解析布局优化

- 去掉“本题解析”下的重复“本题”模块。
- 将解析区调整为“结论”单独占满整行，“关键依据”和“错误点”左右对称展示。
- 小屏幕下解析区自动收为单列，避免三块不对称导致空白。

## v0.6.0 - 解析内容精简

- 去掉“补救”“答案对照”等冗余模块。
- 结论中保留核心对照：“你的选择是 A、B、C，标准答案是 A、B、C。”
- 保留“关键依据”“错误点”“选项解析”，减少无关扩展信息。

## v0.5.0 - 选项解析与判分修正

- 修复错误答案仍显示正确样式的问题。
- 后端对客观题进行确定性判分，减少主观评估波动。
- 正确和错误回答都逐项解析选项，说明正确选项为什么对、错误选项为什么错。
- “本题说明/解析”聚焦当前题考察内容，避免解释下一题。

## v0.4.0 - 提交后停留当前题

- 用户提交回答后停留在当前题页面。
- 当前页直接显示正确与否和本题解析。
- 用户手动点击“下一题”才进入下一题。
- 用户可点击“结束回顾”结束当前关键词复习。

## v0.3.0 - 最近回顾与重点回顾

- 增加“最近回顾”模块，展示已回顾知识点。
- 支持对历史知识点选择“重点回顾”或“全部重来”。
- 重点回顾基于历史低分、错题、遗漏点和不确定内容生成复习重点。
- 使用浏览器 `localStorage` 保存会话和最近回顾数据。

## v0.2.0 - 客观题优先复盘

- 引入判断题、单选题、多选题。
- 客观题数量目标提升到 30 题，以支持不依赖主观题也能完成复盘。
- 主观题改为用户主动点击“主观题”后才生成。
- 修复客观题没有选项时误标为单选题的问题。

## v0.1.0 - 基础知识回顾助手

- 新建知识回顾问答项目。
- 输入知识点后进入一题一答回顾流程。
- 使用 DeepSeek API 生成题目、评估回答并列出依据。
- 增加防幻觉约束：不编造论文、链接、作者、年份或精确引用。
- 支持 `.env.example` 配置 DeepSeek API Key、模型和端口。
